{
  "name": "docs-site-template",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker docs site — generated index + Turnstile/per-doc auth.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.86.0"
  }
}
